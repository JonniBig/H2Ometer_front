import React from 'react';
import { StyledHeader } from './Header.styled';

const Header = () => {
  return (
    <StyledHeader>
      <div>Header</div>
    </StyledHeader>
  );
};

export default Header;
