import styled from 'styled-components';

export const StyledLogout = styled.div`
  backdrop {
    position: fixed;
    top: 50%;
    left: 50%;
    width: ;
    height: ;
    border-radius: 10px;

    background: #fff;
    overflow: hidden;
    overflow-y: auto;
  }
  isHidden {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }
`;
